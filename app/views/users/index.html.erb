<% provide(:title,'all users') %>
<h1>List of users</h1>

<div class="center">
	<table>
		<tr>
			<th>Name</th>
			<th>Email</th>
		</tr>
		<% @users.each do |user| %>
			<tr>
				<td><%= user.name %></td>
				<td><%= user.email %></td>
			</tr>
		<% end %>
	</table>
</div>